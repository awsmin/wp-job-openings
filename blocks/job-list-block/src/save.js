import {InnerBlocks} from "@wordpress/block-editor";
import {Fragment} from "@wordpress/element";

const Save = props => {
	return (
		<Fragment>
			<InnerBlocks.Content />
		</Fragment>
	);
};

export default Save;
